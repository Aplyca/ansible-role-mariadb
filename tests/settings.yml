---
mariadb:
  install_server: True
  cnf:
    client:
      port: 3306
    mysqld:
      bind-address: 0.0.0.0
    mysqldump:
      max_allowed_packet: 16M
  users: 
    - name: root
      host: "%"
  databases:
    - name: app